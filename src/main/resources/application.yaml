spring:
  application:
    name: ms.ticket-manager
  datasource:
    url: jdbc:postgresql://localhost:5441/postgres
    username: postgres
    password: postgres
  jpa:
    open-in-view: false
    show-sql: true
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest